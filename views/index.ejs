<!DOCTYPE html>
<html>
<head>
    <title>TunzyShop Bot Connect</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Poppins, Arial;
            background: radial-gradient(circle at top, #001f3f, #000);
            color: white;
            text-align: center;
        }

        .container {
            margin-top: 60px;
            animation: fadeIn 1s ease-in-out;
        }

        img {
            width: 220px;
            border-radius: 20px;
            box-shadow: 0 0 25px #00aaff;
            animation: float 3s infinite ease-in-out;
        }

        .card {
            margin: auto;
            width: 90%;
            max-width: 450px;
            padding: 25px;
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(10px);
            border-radius: 18px;
            margin-top: 30px;
            box-shadow: 0 0 25px #0099ff50;
        }

        h1 {
            font-size: 32px;
            margin-bottom: 5px;
            color: #00ccff;
        }

        h3 {
            letter-spacing: 1px;
            opacity: 0.8;
        }

        input {
            width: 85%;
            padding: 12px;
            border-radius: 12px;
            border: none;
            margin-top: 15px;
            margin-bottom: 15px;
            outline: none;
            font-size: 17px;
        }

        button {
            width: 90%;
            padding: 12px;
            font-size: 18px;
            background: linear-gradient(90deg, #0088ff, #00ddff);
            border: none;
            border-radius: 12px;
            color: white;
            cursor: pointer;
            box-shadow: 0 0 15px #00bfff;
        }

        button:hover {
            transform: scale(1.05);
            transition: 0.3s;
        }

        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(12px); }
            100% { transform: translateY(0px); }
        }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>

<body>
    <div class="container">
        <img src="/tunzy.jpg" alt="Bot Logo">

        <h1>🔥 TunzyShop Mini Bot</h1>
        <h3>Connect Your WhatsApp Instantly</h3>

        <div class="card">
            <form action="/pair" method="POST">
                <input type="text" name="number" placeholder="Enter WhatsApp Number e.g. 2349067345425" required>
                <button type="submit">Generate Pairing Code</button>
            </form>
        </div>
    </div>
</body>
</html>
